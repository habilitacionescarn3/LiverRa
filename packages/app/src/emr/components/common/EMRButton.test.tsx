// SPDX-License-Identifier: Apache-2.0
// LiverRa test stub — full port deferred pending MediMind context shims.

import { describe, it } from 'vitest';

describe('EMRButton', () => {
  it.todo('renders primary variant with gradient + white label');
  it.todo('renders secondary, danger, ghost, tertiary, text-link variants');
  it.todo('disables interaction while loading=true and shows Loader');
  it.todo('renders leftSection / rightSection icons per iconPosition');
  it.todo('forwards testId, ariaLabel, type, fullWidth props correctly');
  it.todo('keeps label { overflow: visible, height: auto } alongside root padding (CLAUDE.md §Mantine Button Styling)');
});
