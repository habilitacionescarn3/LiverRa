/**
 * LiverRa-specific FHIR profiles + extension URL constants (stub).
 *
 * This barrel will expose:
 *   - StructureDefinition URL constants under
 *     `http://liverra.ai/fhir/StructureDefinition/*` (T037)
 *   - Profile-narrowed TypeScript interfaces for LiverRa's AuditEvent,
 *     ImagingStudy, DiagnosticReport, DocumentReference projections.
 *
 * Empty for now — populated in T037 / T040 / T041.
 */
export {};
