/**
 * Unit tests for liverra/require-emr-button.
 * TODO: wire up eslint RuleTester and real fixtures (T127 follow-up).
 */
describe('liverra/require-emr-button', () => {
  it.todo('flags `import { Button } from "@mantine/core"` in feature dirs');
  it.todo('flags `<Button type="submit">` as primary-action misuse');
  it.todo('allows Button imports in emr/components/common/');
});

export {};
