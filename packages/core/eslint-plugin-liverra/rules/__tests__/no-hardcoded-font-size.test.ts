/**
 * Unit tests for liverra/no-hardcoded-font-size.
 * TODO: wire up eslint RuleTester and real fixtures (T127 follow-up).
 */
describe('liverra/no-hardcoded-font-size', () => {
  it.todo('flags `{ fontSize: "14px" }` inline style');
  it.todo('flags `font-size: 1rem` inside CSS-in-JS string');
  it.todo('ignores `var(--emr-font-sm)` usage');
});

export {};
