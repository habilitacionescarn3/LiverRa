/**
 * Unit tests for liverra/no-any-without-justification.
 * TODO: wire up eslint RuleTester and real fixtures (T127 follow-up).
 */
describe('liverra/no-any-without-justification', () => {
  it.todo('flags `const x: any = foo()` with no adjacent comment');
  it.todo('allows `// any-ok: interop with vendor SDK` preceding `any`');
  it.todo('allows `// TODO: type this properly` preceding `any`');
});

export {};
