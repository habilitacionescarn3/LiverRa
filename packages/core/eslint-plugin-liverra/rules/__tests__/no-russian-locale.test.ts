/**
 * Unit tests for liverra/no-russian-locale.
 * TODO: wire up eslint RuleTester and real fixtures (T127 follow-up).
 */
describe('liverra/no-russian-locale', () => {
  it.todo('flags `ru.json` import paths under translations/');
  it.todo('flags `{ ru: {...} }` object keys under translations/');
  it.todo('ignores words containing "ru" substring like "true"');
});

export {};
