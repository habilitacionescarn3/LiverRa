/**
 * Unit tests for liverra/mantine-button-padding-check.
 * TODO: wire up eslint RuleTester and real fixtures (T127 follow-up).
 */
describe('liverra/mantine-button-padding-check', () => {
  it.todo('flags `<Button styles={{ root: { padding: 8 } }}>`');
  it.todo('ignores `<Button styles={{ label: { height: "auto" } }}>`');
  it.todo('flags `paddingInline` and `paddingBlock` on root');
});

export {};
