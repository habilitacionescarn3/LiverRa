/**
 * FHIR helper utilities barrel.
 *
 * Currently a placeholder — concrete helpers (resource builders, identifier
 * lookup, canonical URL constants) land in later tasks (T036+). This file
 * exists so that downstream imports of `@liverra/core/fhir` resolve cleanly.
 */
export {};
