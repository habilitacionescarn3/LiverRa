# LiverRa ML inference dependencies
# To be populated by first implementation spec (feature 001)
#
# Core expected:
# fastapi>=0.115
# uvicorn[standard]>=0.32
# pydantic>=2.9
# monai>=1.4
# torch>=2.3
# nibabel>=5.3
# pydicom>=3.0
# highdicom>=0.24
# SimpleITK>=2.4
# numpy>=2.0
# scipy>=1.14
#
# Model-specific (loaded lazily):
# tritonclient[all]>=2.50

# ---------------------------------------------------------------------------
# Model integration (docs/plans/model-integration-roadmap.md, Phase 2)
# Pinned to match Triton 24.10's internal PyTorch 2.4.1 — version drift here
# causes silent kernel crashes inside Triton.
# torch+torchvision are installed first via the cu124 index URL; this file
# serves as the dependency manifest for everything else.
# ---------------------------------------------------------------------------
torch==2.4.1
torchvision==0.19.1
monai==1.4.0
uvicorn[standard]>=0.32
alembic>=1.13

# ---------------------------------------------------------------------------
# Declared by T065-T071 (audit chain-of-hashes + PHI scrubber)
# ---------------------------------------------------------------------------
sqlalchemy>=2.0
asyncpg>=0.29
prometheus_client>=0.20
pyahocorasick>=2.1
boto3>=1.35
celery[redis]>=5.4

# ---------------------------------------------------------------------------
# Declared by T155-T166 (Triton client + cascade orchestration + sanity/checkpoint)
# ---------------------------------------------------------------------------
tritonclient[grpc,http]>=2.50
# Pinned <2.0 because monai==1.4.0 requires numpy<2; MONAI 1.4 is itself
# pinned by the roadmap to match Triton 24.10's internal PyTorch 2.4.1.
# Revisit when we move to MONAI 1.5 / PyTorch 2.5+.
numpy>=1.26,<2.0
nibabel>=5.3
SimpleITK>=2.4
# email extra: required by EmailStr in admin invite-user endpoint (api/admin.py).
# Without it, the admin router fails to import and silently disappears via
# main.py:_strict_boot — every /api/v1/admin/* request 404s.
pydantic[email]>=2.9

# ---------------------------------------------------------------------------
# Declared by T044-T050 (Cognito auth + JWKS validation)
# ---------------------------------------------------------------------------
python-jose[cryptography]>=3.3
httpx>=0.27
argon2-cffi>=23.1
psycopg[binary]>=3.2

# ---------------------------------------------------------------------------
# Declared by T126-T139, T405-T411 (observability + error + rate-limit)
# ---------------------------------------------------------------------------
fastapi>=0.115
sentry-sdk[fastapi]>=2.16
opentelemetry-sdk>=1.27
opentelemetry-exporter-otlp>=1.27
opentelemetry-instrumentation-fastapi>=0.48b0
opentelemetry-instrumentation-sqlalchemy>=0.48b0
opentelemetry-instrumentation-celery>=0.48b0
slowapi>=0.1.9
redis>=5.0

# ---------------------------------------------------------------------------
# Declared by T140-T154 (ingest + anonymization + crypto-shred)
# ---------------------------------------------------------------------------
presidio-analyzer>=2.2
presidio-image-redactor>=0.0.53
python-multipart>=0.0.12
python-magic>=0.4.27
# tus protocol is implemented inline in src/api/ingest.py (no tusd-python
# package on PyPI); dependencies above cover the headers + S3 chunking.

# ---------------------------------------------------------------------------
# Declared by T257-T278, T425-T431 (US5 finalize + DICOM-SEG/SR + PDF + PACS)
# ---------------------------------------------------------------------------
highdicom>=0.24
pynetdicom>=2.1
pydicom>=3.0
weasyprint>=62
Jinja2>=3.1
# OCR watermark verification (T275) — tesseract binary must be on PATH.
pytesseract>=0.3.10
pdf2image>=1.17
Pillow>=10.0

# Test runtime
pytest>=8.3
pytest-asyncio>=0.23
