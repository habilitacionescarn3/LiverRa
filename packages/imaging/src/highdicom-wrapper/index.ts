/**
 * highdicom-wrapper — DICOM-SEG + DICOM-SR builders (stub).
 *
 * Wraps the Python `highdicom` library behind a stable TS/Node boundary
 * so the web app can request "build a SEG for these masks, return its
 * SOP Instance UID and S3 URI" without dealing with pydicom internals.
 *
 * Concrete implementation lands with the report-generation tasks
 * (FR-026b / T240-series).
 */
export {};
