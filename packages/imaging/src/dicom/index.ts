/**
 * DICOM parsing + anonymization helpers (stub).
 *
 * Concrete helpers — PHI scrubber, tag reader, SOP Instance UID minter,
 * phase-detection heuristic — are ported from MediMind in Phase 2 tasks
 * (T091, T092, T138). This barrel exists so early consumers can wire
 * the import path now.
 */
export {};
