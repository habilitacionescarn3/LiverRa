/**
 * OHIF v3.9 viewer integration glue (stub).
 *
 * Hosts the LiverRa-specific mode, panel-extension, and hanging-protocol
 * wiring for OHIF. Populated when the viewer ships (US4 task batch).
 */
export {};
